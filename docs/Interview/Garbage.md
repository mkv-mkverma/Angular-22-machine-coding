Angular itself doesn't have a garbage collector. Angular manages the component lifecycle, and the JavaScript runtime's garbage collector handles memory cleanup when Angular objects become unreachable
